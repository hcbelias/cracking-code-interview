import LinkedList from './linked-list';

describe('data-structures/linked-list', function () {
  beforeEach(() => {});
  describe('size method', function () {
    it('should invoke console.table passing internal table as parameter when display method is triggered', () => {});
  });
  describe('clear method', function () {
    it('should invoke console.table passing internal table as parameter when display method is triggered', () => {});
  });
  describe('getLast method', function () {
    it('should invoke console.table passing internal table as parameter when display method is triggered', () => {});
  });
  describe('getFirst method', function () {
    it('should invoke console.table passing internal table as parameter when display method is triggered', () => {});
  });
});
